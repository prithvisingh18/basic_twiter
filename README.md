1) Make sure redis and MySQL are installed.
2) Make necessary changes in config.json to match your local mysql and redis config.
3) npm install
4) run create_db.sql to make database and add data to it
5) npm start
